On arrive sur une interface web avec un formulaire de ping.
On fait des ping sauf qu'on peut pipe ou utiliser ;
par exemple : 127.0.0.1 | ls
on voit qu'il y a un fichier nommé flag5.txt
On le lit avec cat.

FLAG : HTS{mais_nan_yuki}